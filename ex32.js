// ITERAÇÕES COM ARRAY

let clima = ['Ensolarado', 'Nublado', 'Chuvoso', 'Frio', 'Quente']

 clima.forEach(climas => {
     console.log(`Hoje o clima está ${climas}`)
})


/*
 for(let x = 0;x < clima.length; x++){
     console.log(`Hoje o clima está ${clima[x]}`)
}
*/
 

/*
let cont = 0
while(cont < clima.length){
    console.log(`Hoje o clima está ${clima[cont]}`)
    cont++
}
*/